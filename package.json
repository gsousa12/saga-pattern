{
  "name": "saga",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @saga/core dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @saga/database db:generate",
    "db:migrate": "pnpm --filter @saga/database db:migrate",
    "db:push": "pnpm --filter @saga/database db:push",
    "db:studio": "pnpm --filter @saga/database db:studio"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsx": "catalog:",
    "@types/node": "catalog:"
  }
}
